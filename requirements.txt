# Interfaccia grafica
PySide6==6.7.2

# Trascrizione vocale locale (Whisper via CTranslate2), multilingua
faster-whisper==1.0.3

# LLM locale per generazione report (no API, no costi)
llama-cpp-python==0.2.90

# Cattura audio microfono + loopback di sistema (solo Windows)
PyAudioWPatch==0.2.12.7 ; sys_platform == "win32"

# Calcolo numerico (resampling audio)
numpy==1.26.4

# Download modelli
requests==2.32.3

# Export report in formato Word
python-docx==1.1.2

# Packaging in eseguibile Windows
pyinstaller==6.10.0
